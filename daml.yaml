sdk-version: 3.5.2
name: dpm-git-links-demo
source: daml
version: 0.0.1

dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script

data-dependencies:
  # A DAR checked into a git repository, addressed by repo, ref and path.
  # `dpm install` rewrote the branch name `master` into the commit SHA below.
  - "git:github.com/gonzamontiel/test-daml-hello#1c7c5d4776cdb6f57139c7f7fe674e137e79c1e2?path=dist%2Ftest-daml-hello-sdk-3.5.2-lf-2.1.dar"
